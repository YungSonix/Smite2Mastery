# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# Update scripts (old location)
.scripts/
update-builds.js
app/data/builds.json.backup

# Build/deployment
.vercel
web-build/

# Temporary/output files
output.txt
*.log

# Git installer files
Git/
*.exe

# Note: config/supabase.js is now tracked in git
# It uses fallback values, so it's safe to commit
# Environment variables will override fallbacks when set

# Patch notes source files (keep JSON, ignore text sources)
*.txt
!public/**/*.txt

# Backup and temporary files
*.backup
*.bak
*~

# Image files to reduce repository size
# Ignore large asset directories (contains thousands of game asset images)
# These are loaded from GitHub CDN, so they don't need to be in the repo
app/data/Icons/God Info/
app/data/Icons/Wallpapers/
app/data/Icons/Item Icons/

# Keep essential icons that are required by the code
# (Role Icons, Pantheon Icons, Stat Icons, Consumables, Vulcan Mods, Game Modes, Conquest Images are tracked)
