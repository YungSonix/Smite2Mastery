# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# Local / tooling artifacts (do not commit)
eng.traineddata
*.bak
env.download
scripts/god-renders/.vision-tag-*
scripts/god-renders/.vision-tag-STOP

# macOS
.DS_Store
*.pem

# local env files
.env*.local
.env

# Secret templates only — real keys live in .env (local) or Vercel/Expo (deploy)
!.env.example

# typescript
*.tsbuildinfo
tsconfig.json

# generated native folders
/ios
/android

# Update scripts (old location)
.scripts/
update-builds.js
app/data/Backups/
app/data/builds.json.backup
app/data/builds.json.pre-wallpaper-import-*
app/data/God Information/Builds/builds.json.backup
app/data/God Information/Builds/builds.json.pre-wallpaper-import-*

# Build/deployment
.vercel
web-build/

# Temporary/output files
output.txt
*.log

# Git installer files
Git/
*.exe

# Note: config/supabase.js reads EXPO_PUBLIC_* env vars only — no hardcoded keys in repo

# Patch notes source files (keep JSON, ignore text sources)
*.txt
!public/**/*.txt

# Local docs / agent notes (keep on disk; not for GitHub)
*.md
!formative-web/README.md
!docs/trivia-security.md
GOALS.md
BUGS.md
TASKS.md
IMPROVEMENTS.md
ENV_SETUP.md
SECURITY.md
vault.config.json
.cursor/
docs/council/
docs/cursor-agents/

# Local trivia browser-sim outputs
artifacts/trivia-sims/

# Visual audit artifacts (Playwright screenshots + agent queue)
scripts/visual-audit/shots/
scripts/visual-audit/latest/
scripts/visual-audit/queue/
scripts/visual-audit/backups/

# Skins viewer — local dev tool only (npm run skins:viewer)
scripts/skins-viewer/
scripts/skins-viewer-server.js
scripts/skins-viewer.backup*/

# Reddit poll scrape scratch (one-off scripts + HTML/JSON dumps)
.tmp_reddit*
scripts/reddit-*.js
scripts/analyze-reddit-support-poll.js

# Local export / bundle test output
.tmp-*/
.tmp_*
.tmp-*

# git-filter-repo download (local history shrink only)
scripts/git-filter-repo
.tmp-filter-repo-log*.txt

# God render agent scratch batches
scripts/god-renders/.tmp-*
scripts/god-renders/vision-reviews/

# Dated editor / layout backups
*backup-pre-*

# Backup and temporary files
*.backup
*.bak
*~

# Image files to reduce repository size
# Ignore large asset directories (contains thousands of game asset images)
# These are loaded from GitHub CDN, so they don't need to be in the repo
app/data/Icons/God Info/
app/data/Icons/Item Icons/
/tsconfig.json

# Keep essential icons that are required by the code
# (Role Icons, Pantheon Icons, Stat Icons, Consumables, Vulcan Mods, Game Modes, Conquest Images are tracked)

# Voice audio + skin art + legacy icon dumps — on `assets` branch (not slim `master`)
app/data/VoiceAudio/
app/data/Voice Audio/
app/data/NewGodSkins/
app/data/Icons/Wallpapers/
app/data/AspectIcons/
app/data/Icons/Item Icons/
app/data/Icons/God Info/
app/data/Icons/Item Icons Filled/
app/data/Icons/Role Icons/
app/data/Icons/Rarity Icons/
app/data/Icons/Stat Icons/
app/data/Audio Files/

# God render OCR scratch screenshots (local tooling only)
app/data/God Renders/

# Obsidian vault (local mirror)
Vault/
