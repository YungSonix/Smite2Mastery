/**
 * Single Metro entry for `app/data/God Information/Builds/builds.json`.
 * Prefer importing this module instead of hardcoding the path in app code.
 */
const buildsData = require('../app/data/God Information/Builds/builds.json');

module.exports = buildsData;
module.exports.default = buildsData;
