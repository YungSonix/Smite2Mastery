/**
 * Bundled loadout screenshots not yet on GitHub (Metro require — dev/offline).
 * Key: repo-relative path as stored in builds.json `inGame`.
 *
 * Only add entries when the PNG exists on disk — Metro fails at bundle time otherwise.
 */
export const SKIN_LOADOUT_LOCAL_BUNDLES = {};
